rules:
  - name: "Test Rule 1"
    watch:
      - action: include
        patterns:
          - "src/**/*.go"
    commands:
      - "go build"
  - name: "Test Rule 2"
    watch:
      - action: include
        patterns:
          - "web/**/*.js"
    commands:
      - "npm run build"