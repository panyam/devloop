settings:
  max_parallel_rules: 0  # Unlimited (default)
  verbose: true
  prefix_logs: true

rules:
  - name: "rule1"
    watch:
      - action: "include"
        patterns: ["*.txt"]
    commands:
      - "echo 'Rule 1 starting' && sleep 1 && echo 'Rule 1 done'"
      
  - name: "rule2" 
    watch:
      - action: "include"
        patterns: ["*.txt"]
    commands:
      - "echo 'Rule 2 starting' && sleep 1 && echo 'Rule 2 done'"